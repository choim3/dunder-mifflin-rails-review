<h1> Employees </h1>

  <ul>
      <p>
        <% @employees.each do |x| %>
        <li>
          <%= link_to x.full_name, employee_path(x)%> <br />
          </li>
        <% end %>
      </p>
  </ul>
